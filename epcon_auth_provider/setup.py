from setuptools import setup

setup(
    name="epcon_auth_provider",
    version="0.0.1",
    py_modules=['epcon_auth_provider'],
    description="Auth Provider for Synapse to using the Europython website.",
    include_package_data=True,
    zip_safe=True,
    install_requires=[],
)
